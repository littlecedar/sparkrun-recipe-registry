recipe_version: "2"
model: deepreinforce-ai/Ornith-1.0-397B-FP8
model_revision: 8b61f97
model_url: https://huggingface.co/ornith-ai/Ornith-1.0-397B-FP8
runtime: vllm-ray
min_nodes: 4
container: ghcr.io/spark-arena/dgx-vllm-eugr-nightly:latest

metadata:
  description: Ornith-1.0-397B-FP8
  maintainer: Little Cedar Group <ai@littlecedar.net>
  model_params: 397B
  model_dtype: fp8

defaults:
  port: 8000
  host: 0.0.0.0
  tensor_parallel: 4
  pipeline_parallel: 4
  gpu_memory_utilization: 0.9
  max_model_len: 262144
  served_model_name: ornith-397b-fp8
  tool_call_parser: qwen3_xml
  kv_cache_dtype: fp8
  load_format: fastsafetensors
  attention_backend: flashinfer
  reasoning_parser: qwen3
  quantization: compressed-tensors
  max_batched_tokens: 16384
  mamba_cache_dtype: float32
  media_per_prompt: '{"image":4,"video":2}'
  media_encoder_tp_mode: data

env:
  VLLM_ALLOW_LONG_MAX_MODEL_LEN: "1"

command: |
  vllm serve {model} \
    --served-model-name {served_model_name} \
    --quantization {quantization} \
    --enable-auto-tool-choice \
    --tool-call-parser {tool_call_parser} \
    --gpu-memory-utilization {gpu_memory_utilization} \
    --host {host} \
    --port {port} \
    --kv-cache-dtype {kv_cache_dtype} \
    --mamba-cache-dtype {mamba_cache_dtype}
    --max-num-batched-tokens {max_batched_tokens} \
    --load-format {load_format} \
    --attention-backend {attention_backend} \
    --enable-prefix-caching \
    --enable-chunked-prefill \
    --max-model-len {max_model_len} \
    --reasoning-parser {reasoning_parser} \
    --limit-mm-per-prompt {media_per_prompt} \
    --mm-encoder-tp-mode {media_encoder_tp_mode} \
    --trust-remote-code \
    -tp {tensor_parallel} \
    -pp {pipeline_parallel}
