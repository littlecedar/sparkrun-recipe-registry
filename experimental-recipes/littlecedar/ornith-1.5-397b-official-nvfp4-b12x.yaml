recipe_version: "2"

model: ornith-ai/Ornith-1.5-397B-NVFP4
model_revision: c4117555cec0158f908b43cbde6448b9f75984e2

executor: docker
container: ghcr.io/spark-arena/dgx-vllm-eugr-nightly-b12x:latest
runtime: vllm
min_nodes: 4

metadata:
  description: |
    Ornith-1.5-397B

    Chirp Chirp! 🐦 We are introducing Ornith-1.5, a major step toward building foundation models through end-to-end self-improvement.

    Ornith-1.5 extends Ornith-1.0 by expanding the self-improvement loop from scaffold and rollout optimization to jointly optimizing task generation, scaffold construction, and solution rollouts. Rather than relying on a fixed set of human-curated tasks and manually designed harnesses, Ornith-1.5 continuously generates new training tasks, discovers effective strategies for solving them, and improves the policy through reinforcement learning. For more details on the task, harness, and rollout reward design, please refer to our [blog](https://ornith.ai/ornith_1_5.html).
  model_url: https://huggingface.co/ornith-ai/Ornith-1.5-397B-NVFP4
  maintainer: Little Cedar Group <sparkrun@littlecedar.net>

mods:
  - mods/drop-caches
  - mods/instanttensor-hybrid-draft-loader
  - flashinfer-b12x-warmup

env:
  OMP_NUM_THREADS: 1

defaults:
  port: 8000
  host: 0.0.0.0
  tensor_parallel: 4
  pipeline_parallel: 1
  gpu_memory_utilization: 0.5
  #max_model_len: 262144
  max_model_len: 32767
  #max_num_batched_tokens: 16384
  max_num_batched_tokens: 512
  max_num_seqs: 1
  performance_mode: interactivity
  kv_cache_dtype: fp8
  #kv_cache_memory: 21474836480
  #kv_cache_memory: 17179869184
  served_model_name: ornith-ai/Ornith-1.5-397B-NVFP4-b12x
  load_format: instanttensor
  attention_backend: flashinfer
  moe_backend: flashinfer_b12x
  linear_backend: flashinfer_b12x
  mm_encoder_tp_mode: data
  speculative_config: '{"method":"mtp","num_speculative_tokens":3,"max_model_len":32767,"draft_sample_method":"probabilistic","moe_backend":"flashinfer_b12x"}'
  compilation_config: '{"cudagraph_mode":"FULL_DECODE_ONLY","cudagraph_capture_sizes":[1, 2, 4, 8]}'
  # add to compilation_config: only if getting CUDA invalid argument errors:
  # '{"pass_config": {"fuse_allreduce_rms": false}}'

#--kv-cache-memory {kv_cache_memory} \
#--speculative-config '{speculative_config}' \
#--enable-mamba-cache-stochastic-rounding \
command: |
  vllm serve {model} \
    --served-model-name {served_model_name} \
    --host {host} \
    --port {port} \
    --gpu-memory-utilization {gpu_memory_utilization} \
    --max-model-len {max_model_len} \
    --max-num-batched-tokens {max_num_batched_tokens} \
    --max-num-seqs {max_num_seqs} \
    --performance-mode {performance_mode} \  
    --kv-cache-dtype {kv_cache_dtype} \
    --attention-backend {attention_backend} \
    --moe-backend {moe_backend} \
    --linear-backend {linear_backend} \
    --load-format {load_format} \
    --enable-prefix-caching \
    --enable-auto-tool-choice \
    --tool-call-parser qwen3_xml \
    --reasoning-parser qwen3 \
    --trust-remote-code \
    --async-scheduling \
    --mamba-backend flashinfer \
    --mamba-ssm-cache-dtype auto \
    --mamba-cache-philox-rounds 5 \
    --mamba-cache-mode align \
    --mm-encoder-tp-mode {mm_encoder_tp_mode} \
    --compilation-config '{compilation_config}' \
    -tp {tensor_parallel} \
    -pp {pipeline_parallel}

# (Worker_TP0 pid=225) INFO 08-21 03:18:00 [gpu_worker.py:624] CUDA graph memory profiling is enabled (default since v0.21.0). The current --gpu-memory-utilization=0.7500 is equivalent to --gpu-memory-utilization=0.7357 without CUDA graph memory profiling. To maintain the same effective KV cache size as before, increase --gpu-memory-utilization to 0.7643. To disable, set VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS=0.
# (Worker_TP0 pid=225) INFO 08-21 03:18:09 [gpu_worker.py:852] Free memory on device (93.74/121.69 GiB) on startup. Desired GPU memory utilization is (0.75, 91.27 GiB). Actual usage is 68.44 GiB for consumed memory (weights + non-torch), 3.2 GiB for peak activation, and 0.13 GiB for CUDAGraph memory. Replace gpu_memory_utilization config with `--kv-cache-memory=20780792832` (19.35 GiB) to fit into requested memory, or `--kv-cache-memory=23432837120` (21.82 GiB) to fully utilize gpu memory. Current kv cache memory in use is 19.1 GiB.
